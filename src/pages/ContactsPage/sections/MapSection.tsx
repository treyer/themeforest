import Section from "components/elements/Section";
import { SectionType } from "types/index";

const MapSection = () => {
  return <Section type={SectionType.Narrow} marginTop={120}></Section>;
};

export default MapSection;
