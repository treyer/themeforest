import styled from "styled-components";

const AppWrapper = styled.div`
  max-width: 1920px;
  min-height: 100vh;
  margin: 0 auto;

  font-family: "Manrope";
  font-weight: 300;
`;

export default AppWrapper;
